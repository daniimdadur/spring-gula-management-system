spring:
  application:
    name: spring-gms
  datasource:
    url: jdbc:mysql://localhost:3306/gulahub_db
    username: imddur13
    password: P@ssW0rd32!
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        jdbc:
          time_zone: Asia/Jakarta
  data:
    redis:
      host: localhost
      port: 6379
      lettuce:
        pool:
          max-idle: 8
  cache:
    type: redis
    redis:
      cache-null-values: false
      time-to-live: 600000
  docker:
      compose:
        readiness:
          timeout: 10s
app:
  jwt:
    access-secret: ${JWT_ACCESS_SECRET:wdgFBVS9P2joCLJ3eNmknbg3IXlkZBpMOgdpriotWH0=}
    access-expiration: 15
    refresh-expiration: 1440
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
server:
  port: 8080
